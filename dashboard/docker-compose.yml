services:
  dashboard:
    image: dashboard
    container_name: dashboard_app
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "8501:8501"
    env_file:
      - ./.env
      